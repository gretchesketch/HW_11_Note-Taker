// utilities to handle the database

const fs = require("fs");

// this package will generate our unique ids
const uuidv1 = require("uuid/v1");

// ysing class to create an add note method
class Sore {
    addNote(note) {
        const { title, text } = note;
    }
}
